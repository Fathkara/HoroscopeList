import 'package:flutter/material.dart';

class HoroscopeList extends StatelessWidget {
  const HoroscopeList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Burc listesi gelecek"),
    );
  }
}